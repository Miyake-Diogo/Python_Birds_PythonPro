# Containers e iteração
# Tuplas e ID
# Tuplas são imutáveis ficam entre parentesis
tpl = (1,2 )
type(tpl)

dir(tpĺ) # mostra as funções disponiveis para tuplas

tuple(range(6))

# se passar type((6)) retornara somente int
type((6))

type((6,)) #retorna tuple
# 

# Tuplas por serem imutaveis não aceitam 
# operações de modificação

registro = (diogo, 30)
registro2 = (jamile 28)
nome. idade = registro # adiciona o primeiro item da tupla em nome e o segundo em idade
print(nome)
print(idade)
registro + registro2 # retorna a concatenação das tuplas em uma terceira.
# todo objeto em python tem um identificador.
# para chama-lo use a função id
id(registro)





